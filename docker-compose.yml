version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/data
    environment:
      - DATABASE_URL=sqlite:////data/news.db
      - TZ=Europe/Bucharest
      - DAILY_RUN_HOUR=8
    env_file:
      - ./backend/.env
    restart: unless-stopped

  # Web app (optional — can run independently with `cd web && npm run start`)
  # web:
  #   build: ./web
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NEXT_PUBLIC_API_URL=http://backend:8000
  #   depends_on:
  #     - backend

  # Postgres service (uncomment for production)
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: aipulse
  #     POSTGRES_PASSWORD: changeme
  #     POSTGRES_DB: aipulse
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

volumes:
  sqlite_data:
  # pg_data:
